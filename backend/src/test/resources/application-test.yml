spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: test-only-secret-test-only-secret-test-only-secret-32bytes
    access-token-ttl-minutes: 15
    refresh-token-ttl-days: 30
  cors:
    allowed-origins: http://localhost:5173
  s3:
    bucket: chatapp-uploads-test
